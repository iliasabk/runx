name: nitrosend-apply-draft-refuses-intent-before-approval
kind: skill
target: ..
runner: apply-draft
inputs:
  operation: compose_flow
  arguments:
    composition_mode: intent
    goal: Welcome new trial users.
    name: Trial welcome series
  brand_sid: brnd_demo123
env:
  NITROSEND_API_KEY: harness-nitrosend-token
expect:
  status: sealed
  steps: [preflight, blocked]
  step_outputs:
    blocked:
      subset:
        provider_evidence:
          data:
            decision: refused
            provider: nitrosend
            mode: act
            operation: compose_flow
            evidence: null
  receipt: { schema: runx.receipt.v1, state: sealed, disposition: closed }
metadata:
  public_skill: nitrosend
  source_case: apply-draft-refuses-intent-before-approval
  source: operator-journey
